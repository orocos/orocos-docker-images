{
  "build": {
    "event-handlers": ["console_direct+"],
    "merge-install": true
  },
  "test": {
    "event-handlers": ["console_direct+"],
    "executor": "sequential",
    "merge-install": true
  }
}
